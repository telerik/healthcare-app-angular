A user has reported unexpected behavior in the Patient records due to a corrupt ID data type. They had a CSV of patient records but Excel set the Patient ID data type to a decimal. When the data as then automatically uploaded there was unexpected behavior when trying to retrieve the patients by ID.

Can you help us investigate what the expected behavior is for non-integer inputs when retrieving records by patient ID? What input validation exists and what is the expected return types for different scenarios?

Logs and further details like data examples aren't available due to HIPAA
